# Open issues

- Access frontmatter for MDX pages outside of page
- HTML from lazily loaded components (using `import.meta.glob` and `@loadable/components`) are not included in static sites
- Try preact
- Allow to override layout via (via 11ty)
-
